[build]
builder = "nixpacks"

[deploy]
restartPolicyType = "on_failure"
healthcheckPath = "/health"
healthcheckTimeout = 30

# IMPORTANT: keep replicas at 1.
# SQLite on a mounted volume will corrupt with concurrent writers.
numReplicas = 1
