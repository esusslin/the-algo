# RESEARCH PLANE — local only. Never installed on Railway.
#   pip install -r requirements.txt -r requirements-research.txt
#
# Deliberately loose pins: this runs on your machine, not in a container, and
# hard pins break on every new Python release. Exact reproducibility lives in
# the artifact bundle manifest, not here.

# ---- core: needed for warehouse, features, training ----
duckdb>=1.1
pandas>=2.2
scipy>=1.13
statsmodels>=0.14
xgboost>=2.1
shap>=0.46

# ---- experiment tracking ----
mlflow>=2.16

# ---- notebooks / plotting ----
jupyterlab>=4.2
matplotlib>=3.9
seaborn>=0.13

# ---- testing ----
pytest>=8.3
pytest-cov>=6.0

# ---- OPTIONAL: distributional prop models (Week 4+) ----
# ngboost lags new Python releases. Install separately when you need it:
#   pip install ngboost
# ngboost>=0.5.7

# ---- OPTIONAL: Bayesian state-space team ratings (M1, later) ----
# JAX wheels lag new Python releases too. Install separately when you need it:
#   pip install numpyro jax arviz
# numpyro>=0.15
# jax>=0.4.30
# arviz>=0.20
